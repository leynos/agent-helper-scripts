14:  - Installs APT packages, updates certificates, and applies optional global
22:  - Installs user tools such as `uv`, `bun`, `rustup`, cargo tools, Bun global
86:`get-github-tooling` installs `leta`. By default, it also runs:
143:  - Adds the Charm repository and installs the APT `glow` package in the
164:  - Installs Vale through Bun in `get-markdown-tooling`.
167:  - Installs `cargo-llvm-cov` in `get-rust-tooling`.
170:  - Installs Whitaker tooling in `get-rust-tooling`.
173:  - Installs experimental Whitaker pieces when Whitaker is enabled.
244:The system phase installs the `kopia` package when this is set. The home phase
310:  - Install path used by the upstream `tflint` installer.
