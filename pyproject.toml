[project]
name = "agent-helper-scripts"
version = "0.0.0"
requires-python = ">=3.13"

[dependency-groups]
dev = [
  "cmd-mox",
  "cuprum",
  "hypothesis",
  "pytest",
  "pyyaml>=6.0.3",
  "syrupy",
]

[tool.pytest.ini_options]
markers = [
  "slow: exercises an external tool or process boundary",
]
